export function CategoriesPage() {
  return (
    <section className="page">
      <h2>Categories</h2>
      <p className="muted">
        List and CRUD for <code>/api/v1/categories</code> will live here next.
      </p>
    </section>
  );
}
