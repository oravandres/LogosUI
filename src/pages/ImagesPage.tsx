export function ImagesPage() {
  return (
    <section className="page">
      <h2>Images</h2>
      <p className="muted">
        List and CRUD for <code>/api/v1/images</code> will live here next.
      </p>
    </section>
  );
}
